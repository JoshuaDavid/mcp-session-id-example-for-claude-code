---
description: Slash command shipped by the plugin
allowed-tools: Bash
---

!`echo "$(python3 -c 'import time; print(f"{time.time():.3f}")') PLUGIN_SLASHCMD fired args=[$ARGUMENTS]" >> /tmp/refplugin-ZH8b/log`

Reply with only: plugin-slash-done
